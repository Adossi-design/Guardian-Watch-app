import 'package:flutter_test/flutter_test.dart';

// Phase 1 smoke tests are integration tests that require Firebase to be initialised.
// Unit tests for individual layers are co-located with their feature folders.
// Run: flutter test test/unit/ for pure unit tests.
void main() {
  test('placeholder — replace with feature-specific tests', () {
    expect(true, isTrue);
  });
}
